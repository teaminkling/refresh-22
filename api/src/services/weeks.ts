import {createJsonResponse} from "../utils/http";

/**
 * Internal and external handlers for week endpoints.
 */

export const getWeeks = (_params: URLSearchParams, _body: Body): Response => {
  return createJsonResponse();
};

export const getWeek = (_params: URLSearchParams, _body: Body): Response => {
  return createJsonResponse();
};

export const putWeek = (_params: URLSearchParams, _body: Body): Response => {
  return createJsonResponse();
};

export const postWeek = (_params: URLSearchParams, _body: Body): Response => {
  return createJsonResponse();
};
